#include <stdio.h>
#include <stdbool.h>

bool predictTheWinner(int* nums, int numsSize)
{
    printf("%d\n", numsSize);
    return 0;
}

bool catchhim(int* nums, int numsSize)
{
    int list = 0;
    nums[1] = 10 ;
    return 0;
}

int main(void)
{
    int nums[] = {1, 5, 2, 4};
    predictTheWinner(nums, 5);
    catchhim(nums, 3);
    printf("{%d %d %d}\n",nums[0], nums[1], nums[2]);
    return 0;
}